export default function Footer() {
  return (
    <footer className="border-t border-edge py-8 text-center text-xs text-slate-500">
      <p>
        FlightDeals UK · Fares change fast — always verify the price before booking.
      </p>
      <p className="mt-1">
        Some booking links are affiliate links; they never change the price you pay.
      </p>
    </footer>
  );
}
